<h1>Tasks</h1>


<p>Mind dump to get all tasks written down</p>

<div class="table-responsive">
  <table class="table table-hover">
    <thead>
      <tr>
        <th> Name </th>
        <th> Expires at</th>
      </tr>
    </thead>
    <tbody>
      <tr class='js-items'>
        <%= render @user.items %>
      </tr>
    </tbody>
  </table>
</div>

<% if current_user %>
  <%= render 'items/form', item: Item.new, user: @user %>
<% end %>
